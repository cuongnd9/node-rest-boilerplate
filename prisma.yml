endpoint: ${env:PRISMA_ENDPOINT}

datamodel:
  - src/models/user.model.prisma
  - src/models/account.model.prisma

generate:
  - generator: javascript-client
    output: ./src/generated/prisma-client
